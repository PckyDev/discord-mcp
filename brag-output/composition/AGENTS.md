# Launch film source

Use the brag workflow and Hyperframes domain guidance when editing this film.
Read ../brag-plan.md, ../composition-brief.md, and design.md before changing scenes.
Preserve the source-backed product claims and local assets.

Run npm run check and inspect scene snapshots after visual changes. Keep one
paused timeline registered as discord-launch, with a 25-second duration.
The user has requested the finished video, so local rendering is authorized.

Render from this directory with the pinned Hyperframes version. See ../README.md
for poster extraction, encoding and asset credits. Never include credentials or
private Discord content in the illustrative UI.

# Hyperframes brief: paced Discord MCP revision

Create the 74-second revision in brag-plan.md. This is a direct edit request:
longer, much less information at once, and a visible ask/send/result sequence.
User-requested duration and reduced density override short brag defaults.

Use the exact hero wording "Your agent meets Discord" at headline size. Remove
the old kicker, subtitle and rule. Use this settled opening as the new poster.
Keep the actual icon, Manrope, dark Discord surfaces and blurple palette.

Build and Configure each get their own full-screen animated prompt before the
result. A visible pointer presses Send. The build title initially owns the frame,
then moves aside for the server assembly. Messages, forums and events each get a
separate six-second scene, not a grid. Simplify the UI to the action being shown.

Use one paused seek-safe GSAP timeline with explicit clip timing. Reuse the local
assets and text entrance recipe. Use a deterministic per-character prompt reveal,
clear settled reading holds, and subtle click SFX. Extend the original score to
74 seconds; no voice. Static background light is intentional for viewer focus.

Run the full check and inspect opening, typing, sending, build intermediate and
completed result, settings, all three daily tasks and final frame. The user asks
for an edited video, so final local render is authorized. Preserve the old cut.
